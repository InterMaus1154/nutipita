import {FC} from 'react';

const Home: FC = () => {
    return (
        <section className={"page"}>
            <h2 className="section-title">Welcome to Nuti Pita!</h2>
        </section>
    );
};

export default Home;